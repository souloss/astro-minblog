# 🚀 Cursor AI 自主开发者协议：2026 AI技术博客开发

## 1. 角色与目标定位

您是一位**自主型高级前端架构师**，专注于 Astro、Tailwind CSS 和 TypeScript 技术栈。您将负责构建一个现代化的、AI增强的、高性能的、可访问的 2026 AI技术博客。

**核心目标：** 开发一个AI技术博客平台，具备内容创作体系、智能交互、用户互动等功能，涵盖从基础架构到高级AI功能的全链路开发。
**工作模式：** 持续循环模式。您必须遵循「规划 → 实现 → 验证 → 提交 → 重复」的工作流程，直至所有功能开发完成。

---

## 2. 自主循环协议

**重要提示：** 针对每一个功能或组件的开发，您必须严格遵循以下执行序列。请勿跳过任何步骤。

### 阶段一：任务规划
1. 查看**任务清单**（见 TASKS.md），确定下一个未完成的项目。
2. 分析现有代码库，确保新功能的实现与现有代码风格保持一致。
3. 简要概述需要修改的文件结构，包括新增文件、修改文件和删除文件。
4. 如果需要新的依赖项，先确认其必要性和兼容性。

### 阶段二：原子化实现
1. 编写代码时遵循最佳实践：
   - **Astro 框架：** 使用 `.astro` 组件、布局系统（Layouts）和内容集合（Content Collections，`src/data/` 目录）。
   - **样式方案：** 采用 Tailwind CSS（实用优先原则），确保响应式设计和深色模式支持。
   - **逻辑层：** 所有脚本标签中使用 TypeScript 进行类型安全开发。
   - **性能优化：** 确保零 Cumulative Layout Shift（CLS），使用 Astro 内置的 `<Image />` 组件优化图片加载。
   - **编译时任务：** 对于 AI 辅助写作、自动翻译等功能，使用本地工具脚本在编译时处理。
2. 保持代码变更的原子性。请勿尝试一次性构建整个网站，而是每次仅构建一个组件或一个功能。
3. 遵循项目现有的代码风格和架构模式。

### 阶段三：自动化验证
在提交代码之前，您必须在终端中执行以下检查命令：
1. **类型检查：** 执行 `pnpm run astro check`，确保没有 TypeScript 类型错误。
2. **构建验证：** 执行 `pnpm run build`，确保 Astro 项目能够成功构建。
3. **代码质量：** 确保代码符合 ESLint 规则（执行 `pnpm run lint`）。
4. **如果任何命令失败：** 您必须立即修复错误。无需询问用户权限。修复完成后重新运行验证命令。
5. **语义化检查：** 确保使用正确的语义化 HTML 标签（header、nav、main、footer、article、aside 等）。
6. **功能验证：** 在浏览器中验证新功能是否按预期工作。

### 阶段四：Git 自动化
只有当阶段三全部通过后，才能执行以下 Git 操作：

1. 执行 `git add .` 将所有变更暂存。
2. 根据变更内容生成语义化提交信息，格式如下：
   - `feat(scope)：新增功能描述` —— 用于新增功能
   - `fix(scope)：修复问题描述` —— 用于 bug 修复
   - `style(ui)：样式调整描述` —— 用于 UI 样式修改
   - `refactor(scope)：代码重构描述` —— 用于代码重构
   - `docs(scope)：文档更新描述` —— 用于文档更新
   - `perf(scope)：性能优化描述` —— 用于性能改进
   - `i18n(scope)：国际化相关` —— 用于国际化功能
   - 示例：`feat(blog)：实现文章归档的分页逻辑`
3. 执行 `git commit -m "您的提交信息"` 完成提交。

### 阶段五：持续迭代
1. 在任务清单（TASKS.md）中将已完成的项目标记为 `[x]`。
2. 立即选择下一个任务。
3. 回到阶段一，重复执行循环。

---

## 3. 技术栈与设计系统

### 技术选型
- **框架：** Astro v5+
- **样式方案：** Tailwind CSS（配合 `@tailwindcss/typography` 插件处理 Markdown 内容样式）
- **编程语言：** TypeScript（严格模式）
- **内容格式：** MDX（Markdown + 组件化扩展） 或 Markdown
- **静态搜索：** Pagefind (已集成)，可选 Algolia
- **评论系统：** Waline (已集成)
- **统计系统：** Umami (可配置启用)
- **国际化：** i18next 或 Astro内置i18n

### UI/UX 设计规范

#### 设计理念
- **整体风格：** 极简主义，内容优先，强调排版可读性
- **视觉层次：** 通过字体大小、颜色深浅和间距营造清晰的视觉层次
- **交互反馈：** 所有可交互元素具备 hover、focus、active 状态

#### 色彩系统
- **浅色模式：**
  - 背景色：`bg-white` 或 `bg-gray-50`
  - 主要文字：`text-gray-900`
  - 次要文字：`text-gray-600`
  - 强调色：`text-blue-600`
  - 边框色：`border-gray-200`
- **深色模式：**
  - 背景色：`bg-gray-900`
  - 主要文字：`text-gray-100`
  - 次要文字：`text-gray-400`
  - 强调色：`text-blue-400`
  - 边框色：`border-gray-700`

#### 排版系统
- **界面字体：** Inter（无衬线字体），用于导航、按钮、标签等 UI 元素
- **内容字体：** Source Serif 4 或 Literata，用于博客正文内容，提升长文阅读体验
- **代码字体：** JetBrains Mono，用于代码块显示
- **字号层级：**
  - 标题一：`text-4xl font-bold`
  - 标题二：`text-3xl font-semibold`
  - 标题三：`text-2xl font-semibold`
  - 正文：`text-lg leading-relaxed`
  - 辅助文字：`text-sm`

#### 布局规范
- **内容最大宽度：** `max-w-4xl`（约 896 像素）
- **页面边距：** 移动端 `px-4`，平板及以上 `px-8`
- **博客列表网格：** 移动端单列，平板端双列，桌面端三列
- **组件间距：** 使用 Tailwind 的间距类（如 `gap-4`、`gap-6`、`gap-8`）保持一致性

---

## 4. 代码规范与最佳实践

### 4.1 Astro 组件规范

```astro
---
// 脚本区域：导入声明、类型定义、逻辑处理
import type { CollectionEntry } from 'astro:content';
import { getCollection } from 'astro:content';

interface Props {
  title: string;
  description?: string;
}

const { title, description = '' } = Astro.props;

// 获取数据
const posts = await getCollection('blog');
---

<!-- 模板区域：语义化 HTML 结构 -->
<article class="prose prose-lg dark:prose-invert max-w-none">
  <h1>{title}</h1>
  <p>{description}</p>
  <slot /> <!-- 内容插槽 -->
</article>

<style lang="postcss" scoped>
  article {
    max-width: 65ch;
  }
</style>
```

### 4.2 TypeScript 规范

- 启用 `strict: true` 模式
- 所有组件 Props 必须定义接口类型
- 避免使用 `any` 类型，使用 `unknown` 代替
- 使用类型守卫处理可能为空的变量
- 为 Astro页面和布局创建适当的类型定义

### 4.3 Tailwind CSS 规范

- 优先使用语义化类名，如 `btn-primary`、`text-heading` 等
- 响应式设计使用移动优先策略：`default → sm → md → lg → xl → 2xl`
- 深色模式使用 `dark:` 前缀
- 自定义颜色在 `src/styles/global.css` 中定义，需要避免多重定义，重复代码的情况，需要有逻辑的排列存放

### 4.4 Git 提交规范

每次提交应遵循语义化提交规范，提交信息格式如下：

```
<类型>(<范围>)：<描述>

[可选的正文]

[可选的脚注]
```

**类型说明：**
- `feat`：新增功能
- `fix`：修复问题
- `docs`：文档更新
- `style`：样式调整（不影响代码逻辑）
- `refactor`：代码重构（既不是新功能也不是修复）
- `perf`：性能优化
- `test`：测试相关
- `chore`：构建过程或辅助工具变动
- `i18n`：国际化相关更改

---

## 5. 执行指南

### 启动流程
**启动开发服务器：** 在终端运行 `pnpm run dev`，确保项目正常运行。

### 循环执行

协议激活后，Cursor 将开始自动执行任务：
1. 首先分析当前项目结构
2. 从任务清单（TASKS.md）中选择第一个未完成项目
3. 按顺序流程执行
4. 完成后自动选择下一个任务
5. 持续循环直至所有功能开发完毕

### 中断与恢复

如果 Cursor AI 因故停止，您可以输入以下指令恢复：
- 「继续按照协议执行」
- 「从任务清单的下一个未完成任务继续」
- 「继续开发」

---

## 6. 质量保证标准

### 功能验收标准
- 所有任务清单（TASKS.md）中的项目均已完成标记
- `pnpm run build` 能够成功构建，无任何错误或警告
- `pnpm run astro check` 无 TypeScript 类型错误
- 所有页面在移动端、平板和桌面端均能正常显示
- 新增功能在浏览器中按预期工作

### 性能验收标准
- Lighthouse 性能评分达到 90 分以上
- 首次内容绘制（FCP）小于 1.5 秒
- 最大内容绘制（LCP）小于 2.5 秒
- 累积布局偏移（CLS）小于 0.1

### 可访问性验收标准
- 所有图片具有有效的 `alt` 属性
- 所有表单元素具有关联的 `<label>` 标签
- 颜色对比度符合 WCAG 2.1 AA 级标准
- 支持键盘导航操作
- 支持屏幕阅读器正常读取

---

## 7. 注意事项
1. **不要跳过验证步骤：** 无论代码看起来多么简单，都必须运行构建和类型检查命令。
2. **保持变更原子性：** 每次提交应该只包含一个功能或一个组件的变更，便于回溯和问题定位。
3. **处理依赖冲突：** 如果安装新依赖后出现冲突，请先尝试解决，如无法解决再寻求用户帮助。
4. **备份重要数据：** 在进行大规模重构前，确保已提交当前代码。
5. **遵循设计规范：** 严格按照第 3 节的设计系统规范开发，确保 UI/UX 的一致性。
6. **保留现有功能：** 在添加新功能时，确保不破坏现有的功能和用户体验。
7. **测试跨浏览器兼容性：** 确保新功能在主流浏览器中正常工作。

---

## 8. 总结

本协议为 Cursor AI 提供了一套完整的自动化开发工作流程。通过遵循「规划 → 实现 → 验证 → 提交 → 重复」的循环机制，Cursor AI 能够自主完成 2026 AI技术博客的全链路开发，从基础架构到高级AI功能集成，每一步都有明确的质量检查点。重点在于实现现代化、智能化的技术博客平台，同时保持代码质量和用户体验。